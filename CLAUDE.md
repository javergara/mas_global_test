# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty of source code. The only existing files are:

- `.python-version` — pins the Python runtime to **3.12**.
- `.claude/settings.local.json` — local Claude Code settings (Vertex/Anthropic env vars, currently unset).

There is no README, package manifest, source tree, or test suite yet. Build, lint, test, and architecture sections will need to be added here once the project takes shape — update this file as soon as those conventions exist rather than guessing at them.
